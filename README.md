# Flutter App

This workspace contains a minimal Flutter application scaffold.

## Required setup

1. Install Flutter SDK and add it to your PATH.
2. Open the project folder.
3. Run:

```bash
flutter pub get
flutter run
```

## Notes

The current machine does not have the Flutter SDK available on PATH, so the app has not been executed here yet.
